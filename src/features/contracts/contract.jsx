import React from "react";

export default function Contract({
  playerFirstName,
  playerLastName,
  opponentFirstName,
  opponentLastName,
  rules,
  ruleExceptions,
  datetime,
  weightClass,

}) {
  return;
}
